@model ChangeAuthModel

@{
    ViewData["Title"] = LocalizationManager.current["ChangePasswordPageTitle"];
}

<div class="container mt-5">
    <h2 class="text-center mb-4">@LocalizationManager.current["ChangePasswordPageTitle"]</h2>

    <form asp-action="ChangePassword" asp-controller="Profile" method="post" class="card p-4 shadow">
        <div class="form-group mb-3">
            <label asp-for="Password" class="form-label">@LocalizationManager.current["AuthNewPasswordLabel"]</label>
            <input asp-for="Password" class="form-control" placeholder="@LocalizationManager.current["AuthPasswordPlaceholder"]" required />
            <span asp-validation-for="Password" class="text-danger"></span>
        </div>

        <div class="text-center">
            <input type="hidden" asp-for="Email" value="" />
            <input type="submit" value="@LocalizationManager.current["ChangePasswordPageTitle"]" class="btn btn-primary btn-lg" />
        </div>
    </form>
</div>
